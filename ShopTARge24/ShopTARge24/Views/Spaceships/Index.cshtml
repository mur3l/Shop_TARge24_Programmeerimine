@model IEnumerable<ShopTARge24.Models.Spaceships.SpaceshipIndexViewModel>

@{
    int rowNumber = 1;
}

<div>
    <a asp-action="create" asp-controller="Spaceships" type="submit" button class="btn btn-outline-primary mt-3">Create</a>
</div>

<div class="h1">
    Spaceships
</div>

<form method="get">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Nr</th>
                <th scope="col">Name</th>
                <th scope="col">Type</th>
                <th scope="col">Built Date</th>
                <th scope="col">Crew</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var spaceship in Model)
            {
                <tr>
                    <td>@rowNumber</td>
                    <td>@spaceship.Name</td>
                    <td>@spaceship.Classification</td>
                    <td>@spaceship.BuiltDate</td>
                    <td>@spaceship.Crew</td>
                    <td>
                        <div class="action-buttons">
                            <a asp-action="Details" asp-route-id="@spaceship.Id" class="btn btn-outline-primary">Details</a>
                            <a asp-action="Update" asp-route-id="@spaceship.Id" class="btn btn-outline-primary">Update</a>
                            <a asp-action="Delete" asp-route-id="@spaceship.Id" class="btn btn-outline-danger">Delete</a>
                        </div>
                    </td>
                </tr>
                rowNumber += 1;
            }
        </tbody>
    </table>
</form>
